package com.rafi_questions;

public class Contract_bw_Hashcode_Equals {
	/*
	If two objects are equal by .equals() method , then they must have the same hash code. ... The default implementation of hashCode() 
	in Object class returns distinct integers for different objects.
	 */
}
